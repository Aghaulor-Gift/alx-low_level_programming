#include "main.h"
/**
 * main - checks for alphabetic character.
 * Return: Always 1 if c is an alphabet and 0 for anything else
 */
int _isalpha(int c)
{
	for (c = 65; c <= 90; c++)
	{
	for (c = 97; c <= 122; c++)
	{
	return (1);
	}
	return (1);
	}
	return (0);
}

